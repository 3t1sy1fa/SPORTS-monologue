{% extends "layout.njk" %}

{% block content %}
<article class="post-content">
  <h1>{{ title }}</h1>
  <p class="post-meta">
    <strong>{{ category }}</strong> · {{ date | date }}
  </p>
  <div class="post-body">
    {{ content | safe }}
  </div>
</article>
{% endblock %}