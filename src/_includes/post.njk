{% extends "layout.njk" %}

{% block content %}
<article class="post-content">
  <h1>{{ title }}</h1>
  <p><strong>{{ category }}</strong> · {{ date | date("yyyy-MM-dd") }}</p>
  <hr>
  {{ content | safe }}
</article>
{% endblock %}