{% extends "layout.njk" %}

{% block content %}
<article class="post-content">
  <h1 class="post-title">{{ title }}</h1>
  <p class="post-meta">{{ category }} · {{ date | date("yyyy-MM-dd") }}</p>
  <div class="post-body">
    {{ content | safe }}
  </div>
</article>
{% endblock %}