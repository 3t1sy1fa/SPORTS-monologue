---
layout: layout.njk
title: 키움 히어로즈
---

<link rel="stylesheet" href="/style/team-kiwoom.css" />

<div class="team-page">
  <div class="team-split">
    <!-- 좌측: 최근 경기/요약/링크 -->
    <div class="team-left">
      <h2>최근 경기 결과</h2>
      <p class="recent-result">7월 26일 vs 롯데 - 4:3 승</p>
      <p class="summary">불펜진의 호투와 이정후의 결승타로 짜릿한 승리를 거뒀습니다.</p>
    </div>

    <!-- 우측: 분석 -->
    <div class="team-right">
      <h2>구단 분석</h2>
      <div class="team-analysis">
        {% for post in collections.posts %}
          {% if post.data.category == "구단 분석" and "kiwoom" in post.template.inputPath %}
            <div class="post-item">
              <h4><a href="{{ post.url }}">{{ post.data.title }}</a></h4>
              <p class="post-date">{{ post.date | date }}</p>
              <p class="post-desc">{{ post.data.excerpt }}</p>
            </div>
          {% endif %}
        {% endfor %}
      </div>
    </div>
  </div>
</div>