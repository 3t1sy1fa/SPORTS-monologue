---
title: 트윈스타
layout: layout.njk
description: "LG 트윈스 전용 통계 페이지 twinS-TAR – 시즌별 주요 경기 기록과 선수 활약 평점을 제공합니다."
---

{% block styles %}
<link rel="stylesheet" href="/style/twins-tar.css" />
{% endblock %}

{% block content %}
<section class="twins-tar-container">
  <!-- HEADER -->
  <header class="tt-header">
    <div class="tt-header-inner">
      <h1 class="tt-title">TWINS&lt;span&gt;TAR&lt;/span&gt;</h1>
      <p class="tt-subtitle">무적 LG! 끝까지 Twins!</p>
    </div>
  </header>

  <!-- MAIN DASHBOARD -->
  <div class="tt-main">
    <!-- Calendar -->
    <aside class="tt-panel tt-calendar-panel">
      <h2 class="panel-title">이번 달 경기 일정</h2>
      <div class="calendar-wrapper">
        <table class="calendar">
          <thead>
            <tr>
              <th>일</th><th>월</th><th>화</th><th>수</th><th>목</th><th>금</th><th>토</th>
            </tr>
          </thead>
          <tbody>
            {% for week in twinsSchedule | monthCalendarTwins("2025-08") %}
            <tr>
              {% for d in week %}
              <td class="{{ d.cssClass }}">
                {% if d.logo %}
                <img src="/images/s{{ d.logo }}.png" alt="로고" class="cal-logo" />
                {% else %}
                <span>{{ d.day }}</span>
                {% endif %}
              </td>
              {% endfor %}
            </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
    </aside>

    <!-- Players -->
    <section class="tt-panel tt-players-panel">
      <h2 class="panel-title">트윈스 선수단</h2>
      <div class="players-grid">
        {% for p in players | selectattr("teamSlug","equalto","lg") %}
        <a href="/players/{{ p.slug }}/" class="player-card">
          <img src="/images/players/{{ p.slug }}.png" alt="{{ p.name }}" />
          <div class="info">
            <h3>{{ p.name }}</h3>
            <p>{{ p.position }}</p>
          </div>
        </a>
        {% endfor %}
      </div>
    </section>
  </div>

  <!-- STATISTICS -->
  <section class="tt-stats">
    <h2>twinS-TAR 기록</h2>
    <div class="stats-grid">
      {% for s in playerStats | selectattr("teamSlug","equalto","lg") %}
      <div class="stat-card">
        <div class="stat-header">
          <h3>{{ s.playerName }}</h3>
          <time datetime="{{ s.date }}">{{ s.date | date("MM.dd") }}</time>
        </div>
        <p class="game-desc">{{ s.game }}</p>
        <p class="record">{{ s.record }}</p>
        <div class="rating">{{ s.rating }}</div>
      </div>
      {% endfor %}
    </div>
  </section>

  <!-- POPULAR PLAYERS -->
  <section class="tt-votes">
    <h2>인기 선수 TOP 3</h2>
    <div class="vote-cards">
      {% set lgVotes = voteSummary.players | selectattr("playerTeamSlug","equalto","lg") | sort(attribute="playerTotalVotes", order="desc") | slice(0,3) %}
      {% for v in lgVotes %}
      <div class="vote-card">
        <img src="/images/players/{{ v.playerSlug }}.png" alt="{{ v.playerName }}" />
        <div class="vote-info">
          <h3>{{ v.playerName }}</h3>
          <span>{{ v.playerTotalVotes }}표</span>
        </div>
      </div>
      {% endfor %}
    </div>
  </section>
</section>
{% endblock %}
