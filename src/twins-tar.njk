---
title: "트윈스타"
layout: layout.njk
description: "무적 LG, 끝까지 Twins! TwinS-TAR 대시보드"
---

{% block styles %}
<link rel="stylesheet" href="/style/twins-tar.css" />
{% endblock %}

{% block content %}
<section class="twins-dashboard">
  <header class="dashboard-header">
    <p class="slogan">무적 LG, 끝까지 Twins!</p>
    <h1 class="main-title">TWINS DASHBOARD</h1>
  </header>

  <div class="dashboard-layout">
    <!-- 사이드바 -->
    <aside class="sidebar">
      <div class="calendar-card">
        <h3>📅 경기 캘린더</h3>
        <table class="calendar-table">
          <thead>
            <tr>
              <th>일</th><th>월</th><th>화</th><th>수</th>
              <th>목</th><th>금</th><th>토</th>
            </tr>
          </thead>
          <tbody>
            {% set twinsArray = twinsSchedule or [] %}
            {% for week in twinsArray | monthCalendarTwins("2025-08") %}
            <tr>
              {% for day in week %}
              <td class="{{ day.cssClass }}">
                {% if day.logo %}
                  <img src="/images/s{{ day.logo }}.png" class="team-logo" alt="상대팀">
                {% else %}
                  {{ day.day }}
                {% endif %}
              </td>
              {% endfor %}
            </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>

      <div class="recent-matches">
        <h3>⚾ 최근 경기</h3>
        <ul>
          {% for game in twinsArray | slice(-3) | reverse %}
          <li class="match-item {{ game.result }}">
            <span>{{ game.date | date("MM.dd") }} - {{ game.opponent }}</span>
            <span class="score">{{ game.score }}</span>
          </li>
          {% endfor %}
        </ul>
      </div>

      <a href="https://www.lgtwins.com/" target="_blank" class="official-btn">
        🔗 공식 홈페이지
      </a>
    </aside>

    <!-- 메인 -->
    <main class="main-panel">
      <section class="top-players">
        <h2>🔥 인기 선수 TOP 3</h2>
        <div class="top-player-cards">
          {% set lgVotes = (voteSummary.players or []) | selectattr("playerTeamSlug", "equalto", "lg") | sort(attribute="playerTotalVotes", order="desc") %}
          {% for player in lgVotes | slice(0, 3) %}
          <div class="player-card">
            <img src="/images/players/{{ player.playerSlug }}.png" class="player-img" alt="{{ player.playerName }}">
            <h4>{{ player.playerName }}</h4>
            <p>투표 수: {{ player.playerTotalVotes }}</p>
          </div>
          {% endfor %}
        </div>
      </section>

      <section class="all-players">
        <h2>👥 전체 선수단</h2>
        <div class="players-grid">
          {% for player in players | selectattr("teamSlug", "equalto", "lg") %}
          <div class="player-card full">
            <img src="/images/players/{{ player.slug }}.png" class="player-img" alt="{{ player.name }}">
            <div class="info">
              <h4>{{ player.name }} <span>#{{ player.number }}</span></h4>
              <p class="pos">{{ player.position }}</p>
              <p class="note">{{ player.note }}</p>
              <button class="vote-btn" data-type="player" data-slug="{{ player.slug }}">투표하기</button>
            </div>
          </div>
          {% endfor %}
        </div>
      </section>
    </main>
  </div>
</section>

<script type="module">
  import { handleVote } from "/scripts/voteHandler.js";
  document.addEventListener("DOMContentLoaded", () => {
    document.querySelectorAll(".vote-btn").forEach((btn) => {
      btn.addEventListener("click", () => {
        handleVote(btn.dataset.type, btn.dataset.slug);
      });
    });
  });
</script>
{% endblock %}